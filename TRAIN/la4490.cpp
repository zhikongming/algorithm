#include <cstdio>
using namespace std;

const int maxn = 100;
int h[maxn];

int main() {
    int n, k;
    while (scanf("%d%d", &n, &k) == 2 && n) {
        for (int i = 0; i < n; i++) scanf("%d", &h[i]);
         
    }
    return 0;
}

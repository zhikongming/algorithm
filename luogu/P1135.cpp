#include <cstdio>
using namespace std;

const int maxn = 200 + 5;
int k[maxn];
int used[maxn];

int main() {
    int n, a, b;
    scanf("%d%d%d", &n, &a, &b);
    for (int i = 0; i < n; i++) scanf("%d", &k[i]);

    return 0;
}
